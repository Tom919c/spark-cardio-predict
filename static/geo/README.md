# Geo assets

本目录用于阶段二的地图与区域分析资源位。

当前前端采用“本地优先，表格降级”的加载策略：

- `chengdu.json` 是成都市区县边界资源，供 `coverage_level=district` 时离线渲染；
- 地图只用于区域聚合指标比较，不表示居民个人发病概率；
- 新增边界资源时记录来源、更新时间和许可证，不能直接提交未经确认的个人数据。

- 若本地资源缺失，则保留页面空态或降级为表格展示；
- 前端不会在资源缺失时伪造区域业务数据。

如果需要补充省/市级行政区 GeoJSON，请放入本目录并在前端脚本中按需引用。
