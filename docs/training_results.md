# 训练结果

正式训练暂未验收。本文件在阶段一配置、测试和数据链路确认后更新；重新训练后，以 `data/ads/models/active_models.json` 中的指标为准。

| 模型 | 标签 | 指标 |
| --- | --- | --- |
| 心脏事件随机森林 | `label_heart` | 待正式训练 |
| 卒中随机森林 | `label_stroke` | 待正式训练 |

旧版逻辑回归、三分类和多轮随机种子结果不再作为当前版本依据。
